const findMatches = () => {
  //  find interest of user
  // find interests users with at most one shared interest
  // remove duplicates
  // get interest of each user in unique array
  // order them in order of most shared interests
};

export {findMatches};
